{
  "name": "ngx-feat-generator-cli",
  "version": "0.0.1",
  "description": "ngx script to generate feature from a json config input file",
  "main": "./lib/index.js",
  "bin": {
    "ngx-feat-generator": "./lib/index.js"
  },
  "scripts": {
    "start": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/index.ts --file-config ./test/broadcast.json",
    "build": "tsc -p .",
    "global": "npm i -g",
    "test:integration": "run-s clean:integration build global test:integration:start",
    "test:integration:start": "ngx-feat-generator --file-config ./test/broadcast.json",
    "clean": "run-s clean:*",
    "clean:lib": "rm -rf ./lib",
    "clean:integration": "rm -rf ./test/project/src/app/broadcast",
    "reinstall": "rm -rf ./node_modules ./package-lock.json && npm install",
    "publish": "run-s build publish:package",
    "publish:package": "npm publish"
  },
  "author": "Thibaud Dervily",
  "license": "ISC",
  "dependencies": {
    "@types/lodash": "^4.14.149",
    "chalk": "^3.0.0",
    "clear": "^0.1.0",
    "commander": "^4.0.1",
    "figlet": "^1.2.4",
    "lodash": "^4.17.15",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "@angular/cli": "^8.3.21",
    "@types/node": "^13.1.2",
    "nodemon": "^2.0.2",
    "npm-run-all": "^4.1.5",
    "ts-node": "^8.5.4",
    "typescript": "^3.7.4"
  }
}
